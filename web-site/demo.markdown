# DEMO的使用过程
- 下载代码
- (可选)打包代码
    ```shell
    
    ```
- 上传代码
    ```shell
    ```
- 解压代码
    ```shell
    ```
- 进入demo目录
    ```shell
    cd ~/supervision/web-site
    ```
- 启动demo
    ```shell
    ./demo.sh start
    ```
- 等待控制台提示如下成功信息
    ```shell
    xxx 启动成功: http://localhost:8080
    ```
    如果提示其他, 则程序可能未启动完成或启动失败.
- (可选)停止demo
    ```shell
    ./demo.sh stop
    ```